// import { SupportedChainId } from "network";



// Note: if we need to support multi abis,
// we could use factory function to create them.


// interface ContractsFactoryProps {

// }
// function ContractsFactory() {
//   switch(network){
//     case(SupportedChainId.OPTIMISTIC_ETHEREUM):
//         return new OptimismContract()
//       case(SupportedChainId.OPTIMISTIC_ETHEREUM_TESTNET_KOVAN):
//         return new OptimismKovanContract()
//       default:
//         return new OptimismContract()
//   }
// }